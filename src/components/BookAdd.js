import { Navbar, Container, Col } from "react-bootstrap";

const BookAdd = () => {
  const fullYear = new Date().getFullYear()

    return ( 
      <h4 className="text-white text-center">BOOK ADD</h4>
     );
}
 
export default BookAdd;